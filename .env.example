# Optional: enables the supplied Supabase deployment assets.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
# The browser app reads these only when you deliberately expose them via your host configuration.
